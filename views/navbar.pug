nav.navbar.navbar-expand-md.navbar-dark.bg-dark.fixed-top
  a.navbar-brand(href="/") Convention
  button.navbar-toggler(type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation")
    span.navbar-toggler-icon
  #navbarsExampleDefault.collapse.navbar-collapse
    ul.navbar-nav.mr-auto
      li.nav-item
        a.nav-link(href='/booking') Bookings
      if session && session.userid
        li.nav-item.dropdown
          a#navbarDropdown.nav-link.dropdown-toggle(href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false")
            | Profile
          .dropdown-menu(aria-labelledby="navbarDropdown")
            a.dropdown-item(href='/user/' + session.userid) Details
            a.dropdown-item(href='/user/' + session.userid + '/delete') Delete
            a.dropdown-item(href='/user/' + session.userid + '/update') Update
            a.dropdown-item(href='/user/' + session.userid + '/changepassword') Change Password
            .dropdown-divider
            a.dropdown-item(href='/user/logout') Logout
      else 
        li.nav-item
          a.nav-link(href='/user/login') Log In
        li.nav-item
          a.nav-link(href='/user/signup') Sign Up
